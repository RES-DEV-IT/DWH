services:
  pg_res:
    image: postgres:12.6
    environment:
      POSTGRES_USER: resdb
      POSTGRES_PASSWORD: resdb
      POSTGRES_DB: resdb
    volumes:
      - resdb_volume:/var/lib/postgresql/data
    restart: always
    ports:
      - "5431:5432"
    
volumes:
  resdb_volume:
